# Documentation

Read the README in a top-level Lua package before changing that package. Update it when behavior, architecture, public configuration, migration rules, or developer workflow changes materially.

Add a package README for a new top-level subsystem when future maintainers need an ownership or data-flow explanation. Do not create documentation for mechanical edits, and do not use `.context/patterns.md` as a release changelog.
